import Paths from "../../../../path/paths.js"

if( !Paths.main ) {
  document.querySelector("body.ltr > div > header > div.header-wrapper.colored > div.container-fluid.d-flex > div.logo").classList.add('edited')
}